eureka:
  client:
    service-url:
      defaultZone: http://${other.eureka.host}:${other.eureka.port}/eureka
---
spring:
  profiles: eureka-1
server:
  port: 8761
eureka:
  instance:
    hostname: localhost
other:
  eureka:
    port: 8762
    host: localhost
---
spring:
  profiles: eureka-2
server:
  port: 8762
eureka:
  instance:
    hostname: localhost
other:
  eureka:
    port: 8761
    host: localhost